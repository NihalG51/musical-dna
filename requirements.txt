# Musical DNA - Dependencies
# Install: pip install -r requirements.txt
#
# Versions are pinned to a conservative, ABI-consistent stack that runs on
# Streamlit Community Cloud (Python 3.11). The committed models in models/*.pkl
# were generated with exactly these versions, so they unpickle without the
# native/segfault issues that bleeding-edge numpy/pandas builds can cause.

# Music analysis
music21==9.9.1

# Data science
pandas==2.2.3
numpy==1.26.4
scipy==1.13.1

# Machine learning (must match the version that pickled models/*.pkl)
scikit-learn==1.5.2

# Dataframe serialization (pinned: newer builds have segfaulted on the Cloud)
pyarrow==17.0.0

# Visualization
matplotlib==3.9.2
seaborn>=0.13.0

# Dashboard
streamlit==1.57.0

# Utilities
tqdm>=4.66.0          # progress bars for batch processing
